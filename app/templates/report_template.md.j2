# Bug Bounty Report: {{ target }}

**Date:** {{ date }}
**Total Vulnerabilities:** {{ vulns|length }}

---

## Executive Summary
During the automated reconnaissance of **{{ target }}**, we identified **{{ vulns|length }}** potential vulnerabilities using automated scanning tools.

---

## Vulnerability Details

{% for v in vulns %}
### {{ loop.index }}. {{ v.template }}
- **Severity:** {{ v.severity }}
- **Category:** {{ v.category }}
- **Target:** {{ v.host }}
- **Description:** 
  Automated finding by Nuclei template `{{ v.template_id }}`. 
  
  **Evidence:**
  ```
  {{ v.matched_at }}
  ```
  **Software Versions:**
  {% for tech, ver in v.versions.items() %}
  - **{{ tech }}:** {{ ver }}
  {% else %}
  *(No specific version detected)*
  {% endfor %}

  **Infrastructure:** 
  - **ASN:** {{ v.asn }}
  - **Organization:** {{ v.org }}

### Impact (Recommended)
{% if v.severity == 'critical' %}
**CRITICAL RISK**: Exploitation of this vulnerability could lead to full system compromise, data exfiltration, or unauthorized access. Immediate action is required.
{% elif v.severity == 'high' %}
**HIGH RISK**: This vulnerability could allow an attacker to access sensitive data or perform unauthorized actions. Prioritize remediation.
{% elif v.severity == 'medium' %}
**MEDIUM RISK**: Exploitation provides limited access or requires specific conditions. Remediation should be scheduled.
{% elif v.severity == 'low' %}
**LOW RISK**: Minor information disclosure or configuration issue.
{% else %}
**INFO**: Informational finding.
{% endif %}

### Remediation Steps
{{ v.remediation }}

---
{% endfor %}

## Methodology
1. **Passive Recon:** `subfinder` and `amass` (Deep Discovery) for subdomain enumeration and ASN analysis.
2. **Active Probing:** `httpx` for liveness and `whatweb` (Aggressive Fingerprinting) for granular tech stack and version detection.
3. **Vulnerability Scanning:** `nuclei` for template-based scanning.

*Generated by Antigravity Recon Framework*
